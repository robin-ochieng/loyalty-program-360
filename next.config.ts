import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  /* config options here */
  // TODO: Configure UploadThing route handlers and config when wiring uploads.
};

export default nextConfig;
